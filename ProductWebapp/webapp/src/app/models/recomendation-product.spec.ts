import { RecomendationProduct } from './recomendation-product';

describe('RecomendationProduct', () => {
  it('should create an instance', () => {
    expect(new RecomendationProduct()).toBeTruthy();
  });
});
